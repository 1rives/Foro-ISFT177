<!-- Start bottom navbar -->
<nav class="navbar navbar-expand-sm bg-dark bg-gradient py-3" data-bs-theme="dark">
    <div class="container-lg">
        <li class="nav-item list-unstyled pe-2 ps-2">
            <span class="nav-link text-white fw-bold user-select-none" aria-current="page">ISFT 177</span>
        </li>

        <button class="navbar-toggler white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggle" aria-controls="navbarToggle" aria-expanded="false">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse hid" id="navbarToggle">
            <ul class="navbar-nav me-auto gap-2 ps-xl-0">
                <li class="nav-item">
                    <a class="nav-link text-white active" aria-current="page" href="{{ path('app_post') }}">Inicio</a>
                </li>
            </ul>

            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item list-unstyled dropdown">
                    <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        {{ app.user.firstname }} {{ app.user.lastname }}
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('userProfile', {id: app.user.id}) }}">Mi perfil</a></li>
                        <li><a class="dropdown-item disabled" href="#">Configuración</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">Cerrar sesión</a></li>
                    </ul>
                </li>
            {% else %}
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link text-white active" aria-current="page" href="{{ path('app_login') }}">Ingresar</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('app_register') }}">Registrate</a>
                    </li>
                </ul>
            {% endif %}
        </div>

    </div>
</nav>
<!-- End bottom navbar -->